# streamlit_app.py — ULTRA MINIMAL (no external deps)
import streamlit as st
st.set_page_config(page_title="Hello Minimal", layout="wide")
st.title("✅ Ultra Minimal Streamlit App")
st.write("If you can see this, the server is healthy. Now we can swap back to Bloodmap.")
